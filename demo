var heroku = require('heroku_client');

var client = heroku.createClient(API_KEY);

client.getApps(function (err, apps) {
  
});

client.getAppInfo(appName, function (err, info) {
  
});



client.getLogs(appName, {num: 0, ps: 'router', tail: true}, function (err, logStream) {
  
  

});


var logStream = client.logStream(appName, {num: 0, ps: 'router', tail: true, autorestart: true});

logStream.on('error', function (err) {
  // ...
});

logStream.on('close', function () {
  
});

logStream.on('line', function (line) {
  
});

